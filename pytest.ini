[pytest]
testpaths = tests
addopts = --alluredir=reports/allure-results --tb=short
log_level = WARNING
markers =
    smoke: quick sanity checks covering critical paths
    regression: full validation suite covering all data quality dimensions
    e2e: end-to-end checks across the full bronze-silver-gold pipeline
